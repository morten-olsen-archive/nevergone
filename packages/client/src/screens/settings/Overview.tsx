import React, { useCallback } from 'react';
import { useNavigation } from '@react-navigation/native';
import Overview from 'containers/settings/Overview';

const SettingsOverviewScreen = () => {
  return (
    <Overview />
  );
};

export default SettingsOverviewScreen;
